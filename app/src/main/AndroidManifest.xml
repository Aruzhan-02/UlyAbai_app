<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ulyabai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UlyAbai"
        android:windowSoftInputMode="adjustResize|stateAlwaysVisible">
        <activity android:name=".exam.CategoryExamsPage"></activity>
        <activity android:name=".homepage.SingleNewsPage" />
        <activity
            android:name=".HomePage"
            android:label="@string/title_activity_home_page" />
        <activity android:name=".auth.RegistrationPage" />
        <activity android:name=".auth.LoginPage" />
        <activity android:name=".auth.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>